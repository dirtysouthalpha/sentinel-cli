---
name: export
description: Export the current session transcript to a markdown file. Usage: /export [filename.md] (default: session-export.md).
agent: ask
---

# SESSION EXPORT

Export the current conversation to a markdown file. Use the file tool to write sessionToMarkdown(messages) to the specified filename (or "session-export.md" if no filename given). The messages are the current conversation context.
